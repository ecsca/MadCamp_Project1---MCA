<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mca"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TabA"
            android:label="TabA" />
        <activity android:name=".TaskB"
            android:label="TaskB" />
        <activity android:name=".ImagePopup"
            android:label="ImagePopup" />
        <activity android:name=".TabC"
            android:label="TabC" />
        <activity android:name=".Vote"
            android:label="Vote" />
                <activity android:name=".NoticeAlert"
            android:label="NoticeAlert" />
         <activity android:name=".NoticeAlert_Master"
            android:label="NoticeAlert" />
    </application>

</manifest>
